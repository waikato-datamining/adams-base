# File for listing rules for suggesting actors in the flow editor.
#
# Format:
#   rulename = rule
#
# Rules adhere to the syntax described in adams.parser.ActorSuggestion.
# As "rulename" anything apart from "Default" can be used. In order to avoid
# name clashes with other modules, it is recommended to prefix the rules
# with the module name, e.g. "adams-core.rule1=...".
#
# Author: FracPete (fracpete at waikato dot ac dot nz)
# Version: $Revision: 11713 $

# read file

adams-imaging-imagej.ImageJReader1=\
  IF AFTER adams.flow.source.FileSupplier \
  THEN adams.flow.transformer.ImageJReader

adams-imaging-imagej.ImageJReader2=\
  IF AFTER adams.flow.source.SelectFile \
  THEN adams.flow.transformer.ImageJReader

adams-imaging-imagej.ImageJReader3=\
  IF AFTER adams.flow.source.DirectoryLister \
  THEN adams.flow.transformer.ImageJReader

adams-imaging-imagej.ImageJReader4=\
  IF AFTER adams.flow.source.FileSystemSearch \
  THEN adams.flow.transformer.ImageJReader

adams-imaging-imagej.ImageJTransformer1=\
  IF AFTER adams.flow.transformer.ImageJReader \
  THEN adams.flow.transformer.ImageJTransformer

adams-imaging-imagej.ImageJTransformer2=\
  IF AFTER adams.flow.transformer.ImageJTransformer \
  THEN adams.flow.transformer.ImageJTransformer

adams-imaging-imagej.ImageJFeatureGenerator1=\
  IF AFTER adams.flow.transformer.ImageJReader \
  THEN adams.flow.transformer.ImageJFeatureGenerator
  
adams-imaging-imagej.ImageJFeatureGenerator2=\
  IF AFTER adams.flow.transformer.ImageJTransformer \
  THEN adams.flow.transformer.ImageJFeatureGenerator

adams-imaging-imagej.Convert=\
  IF AFTER adams.flow.transformer.ImageJTransformer \
  THEN "adams.flow.transformer.Convert -conversion adams.data.conversion.ImageJToBufferedImage"

adams-imaging-imagej.ImageViewer3=\
  IF AFTER adams.flow.transformer.ImageJTransformer \
  THEN adams.flow.sink.ImageViewer
  
adams-imaging-imagej.ImageViewer4=\
  IF AFTER adams.flow.transformer.ImageJReader \
  THEN adams.flow.sink.ImageViewer
