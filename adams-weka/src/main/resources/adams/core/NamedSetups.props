# Default named setups can be stored in here.
#
# Format:
#   <key>=<classname + options>
#
# Note:
# These setups are also used in the JUnit tests. Whenever modifying the setups
# here, make sure that all the tests still succeed.
#
#
# Author: FracPete (fracpete at waikato dot ac dot nz)
# Version: $Revision$

# data
input_instance_reader=adams.data.io.input.InstanceReader
weka_evaluator_passthrough=adams.data.weka.evaluator.PassThrough
weka_predictions_relativescaler=adams.data.weka.predictions.RelativeNumericErrorScaler

# flow
source_datagenerator=adams.flow.source.WekaDataGenerator
transformer_classifier_j48=adams.flow.transformer.WekaClassifier -classifier weka.classifiers.trees.J48
