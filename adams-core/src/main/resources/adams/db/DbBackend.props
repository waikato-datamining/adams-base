# Defines what class provides the DB backend interface implementations.

# Class name (+ options) that provides the backend
# For example: adams.db.mysql.DbBackend
# leave empty for internal autodetect
Backend=
