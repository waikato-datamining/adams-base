# Project: adams
# Date: 2014-01-23 15:39:35
# User: fracpete
# Charset: UTF-8
#
adams.flow.control.Flow
	-annotation
	Generates a plot from a random sample of numbers and saves\nit to a PNG file, all in the same R script. The filename of the \ngenerated image is then displayed.
	-actor
		adams.flow.standalone.Rserve
	-actor
		adams.flow.standalone.SetVariable
			-var-name
			file
			-var-value
			${EXAMPLE_FLOWS}/output/graph.png
	-actor
		adams.flow.standalone.RStandalone
			-script
			x <- sample(1:100, 200, replace=T)\npng(\"@{file}\")\nplot(x)\ndev.off()
	-actor
		adams.flow.source.Variable
			-var-name
			file
	-actor
		adams.flow.transformer.Convert
			-conversion
				adams.data.conversion.StringToFile
					-placeholderfile
					true
	-actor
		adams.flow.transformer.Convert
			-name
			Convert-1
			-conversion
				adams.data.conversion.FileToString
					-absolute
					true
	-actor
		adams.flow.sink.Display
	-flow-execution-listener
		adams.flow.execution.NullListener

